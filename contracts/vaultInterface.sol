   //SPDX-License-Identifier: MIT
   pragma solidity ^0.8.11;
   
   
   contract test {

   function deposit(address token, uint256 assets) public returns(uint256) {
        token;
    }
    function mint(address token, uint256 assets) public  returns (uint256) {
                token;

    }
    function withdraw(address token, uint256 assets) public returns(uint256) {
                token;

    }
    function earned() public view returns (uint256){
        return (5);
    }

    function redeem(
address token, uint256 shares
    ) public  returns (uint256) {
        return 5;
    }
    function getListSupportedTokens() public view returns (uint256) {
        return 5;
    }
   }
   